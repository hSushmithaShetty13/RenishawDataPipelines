# Legacy Silver notebook

`NB_SILVER_LOAD.py` was the original Silver transform. The POC now uses **Dataflow Gen2** (`04-pipelines/df_silver_load.md`) so the customer sees the Dataflow pattern rather than another notebook. The file is kept here as a reference implementation and as a fallback for tenants without Dataflow Gen2 CI/CD.
